name: flutter_widget_watermark
description: Apply widget-based watermarks to images with single, tiled, and multi-watermark layouts.
version: 0.1.0
homepage: https://fajrul-falah.vercel.app/

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  image: ^4.2.0


dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:

topics:
  - watermark
  - image-processing
  - flutter-widget